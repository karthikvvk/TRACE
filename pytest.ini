[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
filterwarnings =
    # uvicorn still uses websockets.legacy server-side — not our code
    ignore::DeprecationWarning:websockets
    ignore::DeprecationWarning:uvicorn
